const button = document.querySelector('#button1');

const nextPage = () => {
  for (let i = 0; i < 4; i++) {
    const { i } = req.query;
    return i;
  }
};

export default button.addEventListener('click', nextPage);
